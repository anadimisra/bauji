json.extract! certification, :id, :name, :city, :venue, :dates, :schedule, :seats, :unit_price, :venue_map_link, :description, :terms, :created_at, :updated_at
json.url certification_url(certification, format: :json)
